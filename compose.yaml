services:
  vermietluchs:
    build: .
    container_name: vermietluchs
    restart: unless-stopped
    ports:
      - '3001:3001'
    environment:
      VERMIETLUCHS_PORT: '3001'
      VERMIETLUCHS_HOST: 0.0.0.0
      VERMIETLUCHS_DATA_DIR: /data
    volumes:
      - vermietluchs-data:/data

volumes:
  vermietluchs-data:
    name: vermietluchs-data
